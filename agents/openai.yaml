interface:
  display_name: "Painterly Frame Skill"
  short_description: "Source-aware colour, composition and connected brushwork"
  default_prompt: "Use $painterly-frame-skill to turn the supplied image into an original painterly frame: lock composition and scene-owned colour first, then connect the forms with shared-light brushwork while preserving declared anchors."
