interface:
  display_name: "Painterly Frame Skill"
  short_description: "Expressive painted frames with authored colour"
  default_prompt: "Use $painterly-frame-skill to turn the supplied image into an original painterly frame while preserving the declared recognition anchors."
