interface:
  display_name: "Painterly Frame"
  short_description: "Expressive painted frames with authored colour"
  default_prompt: "Use $painterly-frame to turn the supplied image into an original painterly frame while preserving the declared recognition anchors."
